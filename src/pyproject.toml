[project]
name = "earth2-blueprint-app"
version = "0.1.0"
description = "GFS visualization app using earth2-blueprint API"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
]

[tool.uv]
no-build-isolation-package = []
